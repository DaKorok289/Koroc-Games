# Render (and similar hosts) set NODE_ENV=production for both build and runtime.
# npm treats that as a signal to skip devDependencies during install — but this repo's
# build step needs typescript, vite, and all the @types/* packages to build at all.
# This forces devDependencies to install regardless of NODE_ENV.
include=dev
